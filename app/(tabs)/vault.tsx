import { ComingSoon } from '@/components/ui/ComingSoon';

export default function VaultScreen() {
  return (
    <ComingSoon
      title="Asset Vault"
      subtitle="Every raw clip, brief, and deliverable — organised, searchable, and ready to send."
      icon="cube-outline"
    />
  );
}
