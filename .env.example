# Crezo environment variables
# Copy to .env and fill in values

# PostgreSQL (use with Docker: docker-compose up -d)
DATABASE_URL=postgresql://crezo:crezo@localhost:5432/crezo

# API base URL for the Expo app (optional, defaults to localhost)
EXPO_PUBLIC_API_URL=http://localhost:3001/api
